.DEFAULT_GOAL := all

fetch:
	@echo "Nothing to fetch in Docker Compose."

build:
	@echo "Nothing to build in Docker Compose."

install:
	@echo "Installing Docker..."
	@./install.sh

config:
	@echo "Nothing to configure in Docker Compose."

clean:
	@echo "Nothing to clean in Docker Compose."

all: | install
