.DEFAULT_GOAL := all

fetch:
	@echo "Nothing to fetch in Rust."

build:
	@echo "Nothing to build in Rust."

install:
	@echo "Installing Rust..."
	@./install.sh

config:
	@echo "Nothing to configure in Rust."

clean:
	@echo "Nothing to clean in Rust."

all: | install
